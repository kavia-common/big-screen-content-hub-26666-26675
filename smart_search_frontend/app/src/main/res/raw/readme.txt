Place a small sample video here named: sample_video.mp4

The PlayerActivity will attempt to load it from res/raw/sample_video.* via resource name "sample_video".
If no video is present, the app still runs and the player UI remains functional (no crash).
